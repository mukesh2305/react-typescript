export interface Todo {
    id: number,
    todo: string,
    isDone: boolean
}

import React from 'react'
import { useReducer } from 'react'
const ReducerEample = () => {
    return (
        <div>
        </div>
    )
}

export default ReducerEample